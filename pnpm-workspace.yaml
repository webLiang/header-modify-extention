# Single-package repo: pnpm 11 reads allowBuilds / overrides here (strictDepBuilds is on by default).
packages:
  - '.'

allowBuilds:
  '@parcel/watcher': true
  esbuild: true

overrides:
  vite: 8.1.5

confirmModulesPurge: false
