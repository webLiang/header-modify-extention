# Auth/registry-only notes under pnpm 11; allowBuilds / overrides live in pnpm-workspace.yaml.
# Keep RTL's peer package hoisted for Vitest / Testing Library.
public-hoist-pattern[]=@testing-library/dom
